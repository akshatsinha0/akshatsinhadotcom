{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "bun install",
  "buildCommand": "bun run sync:resume && bun run build",
  "outputDirectory": "dist/portfolio/browser",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
