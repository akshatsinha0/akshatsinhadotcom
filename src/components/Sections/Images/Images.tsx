import React,{useState,useEffect} from 'react'
import './Images.css'
const Images:React.FC=()=>{const[open,setOpen]=useState(false);useEffect(()=>{document.body.style.overflow=open?'hidden':'';return()=>{document.body.style.overflow=''}},[open]);const urls=Array.from({length:18},(_,i)=>`https://images.unsplash.com/photo-${1582719471800+i}?auto=format&fit=crop&w=1600&q=80&sig=${i}`);return(<section className="images-section"><div className="images-container"><header className="images-hero"><h2 className="images-title">Hire are the images you will like the most</h2><p className="images-subtitle">Scroll to see them <span className="arrow">↓</span></p><button className="view-btn" onClick={()=>setOpen(true)}>View</button></header><div className="images-grid">{urls.map((_,i)=>(<figure key={i} className="img-card"><img src={`https://source.unsplash.com/1600x900/?aurora&sig=${i}`} alt={`aurora-${i}`}/></figure>))}</div></div>{open&&(<div className="images-fullscreen" role="dialog" aria-modal="true"><button className="fs-close" aria-label="Close" onClick={()=>setOpen(false)}>×</button><div className="fs-grid">{urls.map((_,i)=>(<figure key={i} className="img-card"><img src={`https://source.unsplash.com/1800x1200/?aurora,northern-lights&sig=${i}`} alt={`aurora-full-${i}`}/></figure>))}</div></div>)}</section>)}
export default Images
